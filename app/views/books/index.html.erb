<div>
  <%= render 'users/user', users: @users %>
</div>
<div>
  <%= render 'books/book', books: @books %>
</div>
<div>
  <h1>Books</h1>
  <table>
    <thead>
      <!--<th></th>-->
      <th>Title</th>
      <th>Opinion</th>
    </thead>
    <tbody>
      <%= @books.each do |book| %>
      <tr>
        <!--<td>-->
        <!-- <%= link_to user_path(book.user) do %>-->
        <!-- <%= attachment_image_tag book.user, :profile_image %>-->
        <!-- <% end %>-->
        <!--</td>-->
        <td>
          <%= link_to book.title, "user_path(book.id)" %>
        </td>
        <td><%= @book.body %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>